The mentioned `sudo npm install -g i3-layout-viewer` doesn't work and I can't be assed to figure out why.

After installing, manually copy all code in src/* and bin/* to /usr/lib/node_modules/i3-layout-viewer/{bin,src}/
